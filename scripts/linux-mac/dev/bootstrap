#!/bin/bash
set -euo pipefail

AccountId="${1:-}"
Region="${2:-}"

if [ -z "$AccountId" ] || [ -z "$Region" ]; then
  echo "[dev] Usage: ./scripts/linux-mac/dev/bootstrap <ACCOUNT_ID> <REGION>" >&2
  exit 1
fi

uv run cdk bootstrap "aws://$AccountId/$Region"
