#!/bin/bash
set -euo pipefail

ConfigPath="${OPENCLAW_CONFIG_PATH:-config/dev.json}"
if [ ! -f "$ConfigPath" ]; then
  echo "[dev] Config file not found: $ConfigPath" >&2
  exit 1
fi

export OPENCLAW_CONFIG_PATH="$ConfigPath"
uv run cdk deploy --require-approval never
