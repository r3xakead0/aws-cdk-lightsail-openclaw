#!/bin/bash
set -euo pipefail

AccountId="${1:-}"
Region="${2:-}"

if [ -z "$AccountId" ] || [ -z "$Region" ]; then
  echo "[prod] Usage: ./scripts/linux-mac/prod/bootstrap <ACCOUNT_ID> <REGION>" >&2
  exit 1
fi

uv run cdk bootstrap "aws://$AccountId/$Region"
